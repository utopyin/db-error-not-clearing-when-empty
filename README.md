# Minimal reproduction for Tanstack DB

**Issue**: the error is not being cleared properly when a collection returns an empty list

### Install the reproduction
- run `git clone https://github.com/utopyin/db-error-not-clearing-when-empty`
- run `pnpm install`
- run `pnpm run dev`
